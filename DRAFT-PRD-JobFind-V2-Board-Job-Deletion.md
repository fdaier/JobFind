# DRAFT PRD — JobFind V2：申请看板岗位删除

> 状态：草案。仅供本轮评审；确认后才进入正式建设文档和产品代码。
>
> 日期：2026-09-12

## 1. 决策摘要

为申请看板中的每一个岗位增加可随时执行的删除能力，**所有阶段一视同仁**，包括 `关注中`、`待投递`、`已投递`、`笔试`、`面试`、`录用` 和 `已淘汰`。

删除入口不放在看板列头，也不做成岗位卡片上的主按钮。用户先打开岗位详情，再在底部操作区看到低强调度的“删除该岗位”文字按钮；点击后必须二次确认。这样既能修复终态岗位无法清除的问题，也保留用户通常希望长期留存面试过程的预期。

本期删除为本地数据的永久删除：岗位本身、时间线、面试复盘和该岗位内的 Agent 建议会一并删除；材料中心中对该岗位的关联引用会同步移除。暂不做回收站、批量删除和跨设备恢复。

## 2. 背景与问题确认

JobFind 当前的申请看板有 7 个阶段：

| 阶段 | 状态性质 |
| --- | --- |
| 关注中、待投递、已投递、笔试、面试 | 进行中 |
| 录用、已淘汰 | 终态 |

现有岗位详情抽屉只提供“推进到下一阶段”和“标记淘汰”。当岗位进入 `录用` 或 `已淘汰` 后，阶段推进按钮会被清空；同时全局状态没有 `deleteJob` 操作。因此，终态栏目中的岗位无法移除，其他栏目里的岗位也没有删除路径。

用户的真实使用习惯不是频繁删除历史申请：面试内容、过程记录和结果通常值得保留。删除的高价值场景是误添加岗位、重复录入、测试数据或不再需要保存的异常记录。因此设计重点是“始终可达、低存在感、避免误删”，而不是提高删除频率。

## 3. 目标与边界

### 3.1 目标

1. 用户能从任意阶段的岗位详情中删除该岗位。
2. `录用` 和 `已淘汰` 不再成为无法管理的终点。
3. 删除入口保持次要，不干扰推进阶段、查看 Agent 建议和保留历史记录等高频动作。
4. 删除后看板、首页任务、漏斗和复盘分析立即基于剩余岗位重新计算。
5. 用户刷新页面后，已删除岗位不会因 localStorage 恢复而重新出现。

### 3.2 不在本期范围

- 回收站、撤销删除、定期自动清理。
- 批量删除、看板多选或拖拽删除。
- “归档”替代删除；`录用`、`已淘汰`仍保留为正常阶段。
- 后端、账号、跨端同步和审计日志。
- 删除材料实体；本期只解除材料对被删除岗位的引用。

## 4. 产品与交互方案

### 4.1 唯一入口：岗位详情抽屉的低强调度危险操作

岗位卡片继续只承担“打开详情”的职责，不增加常驻垃圾桶图标，避免误触与卡片信息拥挤。

在 `JobDetailSheet` 底部操作区中，保留既有的阶段推进操作和“关闭详情”。在它们下方以分隔线隔开新增一行：

```text
推进阶段
[推进到面试] [标记淘汰]
────────────────────────
[关闭详情]                         删除该岗位
```

“删除该岗位”使用 `ghost` / 文字型危险样式：小号、低饱和红灰色、无填充背景；仅在 hover 与键盘 focus 时增强可见性。它不应使用醒目的实心红色主按钮，也不应置于卡片、列头或页面主操作区。

该入口对全部 7 个阶段始终显示。即使岗位处于“录用”或“已淘汰”，用户也能先打开详情再删除。

### 4.2 二次确认

点击“删除该岗位”后打开确认对话框，而不立即删除。

| 元素 | 文案 / 行为 |
| --- | --- |
| 标题 | 删除「{公司} · {岗位}」？ |
| 说明 | 将永久删除该岗位、申请时间线和面试复盘，并解除关联材料中的岗位引用。此操作无法恢复。 |
| 次按钮 | 取消：关闭对话框，不改变任何数据 |
| 危险按钮 | 删除岗位：执行删除 |
| 默认焦点 | 取消按钮，避免 Enter 误删 |
| 键盘与无障碍 | Escape / 遮罩关闭等同取消；对话框有可读标题和说明；危险按钮名称包含“删除岗位” |

确认成功后关闭确认对话框与岗位详情抽屉，返回原看板位置。若被删除的是该阶段最后一条记录，该列立即显示既有空状态“这个阶段还没有岗位”。

本版不提供 Toast 撤销。删除前的明确确认比短暂撤销更符合“低频、慎重处理”的定位；未来若加入回收站，再统一设计恢复语义。

### 4.3 数据一致性

删除是一项 Store 内的原子状态更新，按以下顺序产生同一份新状态：

1. 从 `jobs` 移除匹配 `jobId` 的岗位。
2. 遍历 `materials`，从每个 `boundJobIds` 移除该 `jobId`，不删除材料本身。
3. 若被删除岗位正处于详情打开状态，将 `selectedJobId` 设为 `null`。
4. 复用现有持久化 effect 保存新的 `jobs` 与 `materials` 至 localStorage。

岗位的 `timeline`、`interviewNotes`、风险标签和 Agent 建议都包含在岗位对象内，随对象删除自然消失；不新增独立的清理存储键。首页任务、漏斗、渠道复盘和面试复盘均从 `jobs` 派生，因此无需额外缓存失效逻辑。

### 4.4 异常与边界状态

| 场景 | 预期结果 |
| --- | --- |
| 删除进行中岗位 | 删除成功，看板计数与派生任务立即更新 |
| 删除录用岗位 | 删除成功，录用列计数与漏斗更新 |
| 删除已淘汰岗位 | 删除成功，已淘汰列计数与漏斗更新 |
| 删除有面试复盘的岗位 | 岗位及其面试复盘一并消失 |
| 删除有关联材料的岗位 | 材料仍保留，但不再含该岗位 ID |
| 取消或按 Escape | 不写入 Store / localStorage，详情保持打开 |
| 选中的岗位已因删除消失 | 详情抽屉关闭，不渲染陈旧详情 |
| 刷新页面 | 被删岗位不从 localStorage 恢复 |

## 5. 技术设计

### 5.1 受影响文件

| 文件 | 改动 |
| --- | --- |
| `src/hooks/use-jobfind-store.tsx` | 在 Store 契约中新增 `deleteJob(jobId: string): void`；原子更新岗位、材料关联与选中状态。 |
| `src/hooks/use-jobfind-store.test.tsx` | 覆盖删除、材料关联清理、选中态关闭及后续持久化。 |
| `src/components/job-detail/job-detail-sheet.tsx` | 新增低强调度删除入口、受控确认状态和确认对话框；不按岗位阶段隐藏。 |
| `src/app/board/page.test.tsx` | 覆盖从各阶段岗位详情触发、取消与确认删除的完整交互。 |
| `src/components/board/kanban-board.test.tsx` | 覆盖删除后列计数与空状态变化（如现有集成测试不足）。 |

不需要修改 `Job`、`JobStage` 类型，也不需要新增 localStorage key；`saveJobs` / `saveMaterials` 已由 Store 的持久化 effect 统一调用。

### 5.2 Store 接口

```ts
interface JobFindStoreValue {
  // existing members
  deleteJob: (jobId: string) => void;
}
```

实现约束：

- 不原地修改 `jobs`、`materials` 或其嵌套数组。
- `jobId` 不存在时返回原状态，避免无意义写入。
- 仅清理 `Material.boundJobIds`，不触碰 `completedTaskIds`：其中任务 ID 不是稳定的岗位 ID，且任务会从剩余岗位重新派生。
- 不调用阶段推进逻辑，不追加“已删除”时间线事件；删除后该时间线已不存在。

### 5.3 组件状态

`JobDetailSheet` 增加本地布尔状态，例如 `isDeleteDialogOpen`。打开详情、切换所选岗位或关闭抽屉时应复位为 `false`，防止确认框残留到下一个岗位。

确认按钮点击时先调用 `deleteJob(selectedJob.id)`，再关闭确认框。由于 Store 同步清空 `selectedJobId`，详情抽屉会自动关闭；组件不依赖临时缓存的完整岗位对象完成后续渲染。

## 6. 验收标准

1. 看板 7 个阶段中的任一岗位，打开详情后都能看到“删除该岗位”。
2. 删除入口在视觉上弱于“推进阶段”和“关闭详情”，且岗位卡片上没有常驻删除按钮。
3. 点击删除入口只打开确认对话框，不会立即改变看板数据。
4. 对话框准确显示公司与岗位，并说明会删除时间线、面试复盘及解除材料关联，且不可恢复。
5. 点击“取消”、关闭图标、遮罩或 Escape 后，岗位及详情均保持不变。
6. 点击“删除岗位”后，该岗位立即从所在列消失，列计数、首页派生任务、漏斗和复盘统计同步更新。
7. 删除终态岗位（录用、已淘汰）与删除进行中岗位的体验一致。
8. 删除有关联材料的岗位后，材料仍存在且 `boundJobIds` 不含被删除岗位 ID。
9. 刷新页面后，被删除岗位不会恢复。
10. 完整测试、类型检查与构建通过；不引入后端、依赖包或非本期功能。

## 7. 实施与验证计划

### 阶段 1：先补失败测试

1. 在 Store 测试中选择一条带 `boundMaterialIds` 的岗位并调用 `deleteJob`。
2. 断言岗位数量减少、目标岗位不存在、材料引用被移除、`selectedJob` 变为 `null`。
3. 在看板页交互测试中分别选用一条“录用”和一条“已淘汰”岗位，断言两者均有删除入口。
4. 断言点击删除后确认框出现；取消不改变数量；确认后计数与卡片数变化。

### 阶段 2：最小实现

1. 实现 `deleteJob` Store action 与不可变数据清理。
2. 在详情抽屉中接入低强调度入口与 Radix `Dialog` 确认框。
3. 保持既有阶段推进、关闭详情和 card click 行为不变。

### 阶段 3：验证

```powershell
npm.cmd run test -- src/hooks/use-jobfind-store.test.tsx src/app/board/page.test.tsx src/components/board/kanban-board.test.tsx
npm.cmd run test
npm.cmd run typecheck
npm.cmd run build
```

人工 smoke path：依次删除一条“待投递”、一条“录用”、一条“已淘汰”岗位，检查确认与取消分支、材料引用以及页面刷新后的状态。

## 8. 发布说明（面向用户）

申请看板现已支持删除误添加或不需要保留的岗位。打开任意岗位详情，在底部选择“删除该岗位”并确认即可；录用和已淘汰岗位同样可以删除。为避免误删，删除会要求二次确认，且会同时移除该岗位的申请记录与面试复盘。

## 9. 后续可评估项（不纳入本期）

- 回收站（例如保留 30 天）与恢复入口。
- 删除后的短时 Undo，但需要同时恢复材料关联与所有岗位内历史数据。
- 归档视图，用于保留完成申请但从主看板隐藏。
- 批量选择和批量操作。
