# DRAFT-PRD-JobFind V2：迭代复盘中心结构化输入

> 状态：草案，等待确认后再合入正式建设文档。

## 1. Summary

本需求定义 JobFind V2 阶段的“迭代复盘中心”升级。目标是把当前偏展示型的复盘中心，升级为一个用户能主动输入问题、回答、备注，并持续沉淀 Agent 策略记忆的结构化工作台。

本轮重点不是做完整聊天 Agent，而是先解决输入路径不明显的问题。产品给出复盘问题提示，用户用更低成本的选项化表单完成 QA 记录，并可以点击加号添加新的 QA 和问题备注。

## 2. Contacts

| Name | Role | Comment |
|---|---|---|
| 产品负责人 | AI 产品经理候选人 | 负责定义复盘中心 V2 的目标、字段、流程和验收标准 |
| Codex | 原型开发协作者 | 负责根据 PRD 产出后续设计、实现和验证 |
| 面试评审 | 目标评审方 | 关注 V2 是否延续“AI 求职项目经理”定位，而不是变成普通笔记 |
| 大学生求职用户 | 目标用户 | 需要在投递、面试和阶段变化后快速复盘，并让下一轮行动更清楚 |

## 3. Background

### 3.1 Context

JobFind 当前的产品主线是“学生的 AI 求职项目经理”。Stage 1 已经完成求职作战台、申请看板、岗位详情、材料中心、规则型 Agent 和复盘中心的基础展示。

当前复盘中心已经包含：

- Agent 策略记忆。
- 渠道复盘。
- 面试复盘沉淀。
- 转化漏斗。

但当前页面更像“系统帮我看结果”，用户主动输入复盘内容的路径不够明显。用户看到复盘中心后，可能不知道应该从哪里开始写，也不知道应该回答什么问题。

### 3.2 Pain Point

核心痛点：

> 输入复盘问题和回答的路径不明显。

具体表现：

- 复盘中心更偏展示，缺少清晰的“开始复盘”入口。
- 用户不知道该写什么，容易停在空白状态。
- 问题、回答、备注没有明确的信息结构。
- 新增 QA 的动作不突出，难以形成连续复盘。
- Agent 策略记忆缺少用户输入来源，容易显得像静态结论。

### 3.3 Why Now

V2 阶段需要从“Agent 能诊断和展示”走向“Agent 能陪用户沉淀判断”。复盘中心正好是连接历史申请和下一轮行动的地方。

如果复盘中心只有指标展示，产品会停留在数据看板。如果加入结构化 QA 输入，它就能把用户的真实想法、面试反馈、渠道判断和材料经验沉淀为下一轮策略。

### 3.4 Constraint

本轮仍然保持原型范围，不做过重建设。

本轮不做：

- 完整自由聊天 Agent。
- 多人协作复盘。
- 复杂富文本编辑器。
- 真实后端和账号系统。
- 自动生成最终求职报告。
- 自动替用户修改简历或发送消息。

本轮优先做：

- 明确入口。
- 产品预设问题。
- QA 结构化输入。
- 加号新增 QA。
- 问题备注。
- 本地持久化。
- Agent 根据复盘内容生成轻量策略记忆。

## 4. Objective

### 4.1 Product Objective

把复盘中心从“看结果的页面”升级为“写复盘、沉淀判断、反哺下一轮行动的工作台”。

### 4.2 Why It Matters

求职复盘不是为了写总结，而是为了减少下一轮试错。用户需要知道：

- 哪些渠道值得继续投。
- 哪类岗位更容易推进。
- 哪些面试问题反复出现。
- 哪版材料效果更好。
- 哪些行动下次应该提前做。

结构化 QA 能降低用户复盘成本，也能让 Agent 有更可靠的上下文。

### 4.3 Key Results

| Key Result | Measurement |
|---|---|
| KR1：入口清晰 | 用户进入复盘中心后 5 秒内能看到“开始复盘”或问题填写区域 |
| KR2：输入成本降低 | 用户不用自己想问题，也能从产品预设问题中开始回答 |
| KR3：QA 可连续扩展 | 用户可以点击加号新增至少 1 条自定义 QA |
| KR4：备注可沉淀上下文 | 每个问题都支持填写备注，用于记录背景、证据或下次提醒 |
| KR5：复盘能反哺 Agent 记忆 | 保存 QA 后，Agent 策略记忆能展示来自复盘输入的结论或下一步建议 |
| KR6：本地状态可靠 | 刷新页面后，用户新增的 QA 和备注仍然存在 |

## 5. Market Segment(s)

### 5.1 Primary Segment

正在找实习或校招岗位的学生，尤其是已经投递多个岗位，并开始经历笔试、面试、拒绝、Offer 或长期无反馈的人。

他们不是只需要记录“发生了什么”，而是需要理解“这说明什么，下一轮怎么调整”。

### 5.2 Core User Jobs

- 我刚面完试，想趁记忆还清楚，把问题和回答记下来。
- 我投了很多岗位，想知道哪个渠道值得继续维护。
- 我想记录某个问题为什么重要，避免下次忘记。
- 我想根据产品给的问题提示快速复盘，而不是从空白页开始。
- 我想新增自己的复盘问题，形成个人求职策略库。

### 5.3 User Constraints

- 用户复盘时通常很累，不愿写长篇总结。
- 用户不知道标准复盘应该包含哪些问题。
- 用户需要可扫读、可编辑、可继续追加的输入方式。
- 复盘内容可能很零散，必须允许先写粗略版本。
- 原型阶段没有真实后端，需要使用 localStorage 保存。

### 5.4 Non-Target Users For This Version

本轮不优先服务：

- HR、猎头或招聘团队。
- 已有复杂 CRM 工作流的资深职场人。
- 需要多人批注和协同复盘的团队。
- 需要导出完整报告或同步到外部知识库的用户。

## 6. Value Proposition(s)

### 6.1 Core Value Proposition

迭代复盘中心让用户不用从空白页开始复盘。产品先给出问题，用户填写答案和备注，再由 Agent 把这些内容沉淀成下一轮求职策略。

### 6.2 Customer Gains

| User Need | V2 Response |
|---|---|
| 我不知道该复盘什么 | 产品预设问题提示 |
| 我只想快速写几句 | 问题栏、回答栏和备注栏分开，降低输入压力 |
| 我还有自己的问题 | 点击加号新增 QA |
| 我想补充背景 | 每个问题支持备注 |
| 我想让复盘有用 | Agent 从 QA 中提炼策略记忆和下一步建议 |

### 6.3 Pain Relievers

- 把“空白复盘”变成“回答问题”。
- 把“长篇总结”变成“多条 QA”。
- 把“想到哪里写哪里”变成“问题、回答、备注”。
- 把“复盘后就结束”变成“反哺下一轮行动”。

### 6.4 Competitive Difference

普通看板只记录流程状态。普通笔记只保存文本。JobFind 的复盘中心要做的是：

- 结合申请池数据和用户复盘输入。
- 让用户通过结构化 QA 低成本沉淀经验。
- 让 Agent 把复盘转成策略记忆和行动建议。

这延续了 JobFind 的核心定位：不是记录工具，而是学生的 AI 求职项目经理。

## 7. Solution

### 7.1 UX / Prototypes

#### 7.1.1 Page Position

复盘中心仍作为一级导航页面存在，页面名建议从“复盘中心”升级为：

> 迭代复盘中心

页面描述建议：

> 把每次投递、面试和反馈沉淀成下一轮策略。先回答关键问题，再让 Agent 提炼可复用判断。

#### 7.1.2 First Screen Layout

首屏从上到下组织为：

1. 页面标题和一句话说明。
2. 顶部 Agent 策略记忆摘要。
3. “开始本轮复盘”输入区。
4. 产品预设问题列表。
5. 已保存 QA 列表。
6. 渠道复盘、面试复盘、转化漏斗作为下方分析区。

#### 7.1.3 Core User Flow

```mermaid
flowchart LR
    A["进入迭代复盘中心"] --> B["看到产品预设问题"]
    B --> C["填写回答"]
    C --> D["填写问题备注"]
    D --> E["点击保存"]
    E --> F["QA 进入复盘记录"]
    F --> G["Agent 更新策略记忆"]
    B --> H["点击加号"]
    H --> I["新增自定义问题"]
    I --> C
```

#### 7.1.4 QA Input Card

每条 QA 使用一个紧凑卡片。

字段：

| Field | Type | Required | Description |
|---|---|---|---|
| 问题 | Text input / preset select | Yes | 产品给出默认问题，用户也可以编辑 |
| 回答 | Textarea | Yes | 用户填写本次复盘回答 |
| 备注 | Textarea | No | 记录证据、背景、下次提醒或面试官反馈 |
| 标签 | Optional segmented control | No | 渠道、材料、面试、节奏、岗位方向 |
| 关联岗位 | Optional select | No | 可关联到某个岗位，首版可以先用下拉或静态选择 |

按钮：

- 保存。
- 取消。
- 加号：新增一条 QA。
- 删除：仅对未保存或用户新增 QA 开放，避免误删默认问题。

#### 7.1.5 Product-Prompted Questions

首批产品预设问题建议：

| Category | Question |
|---|---|
| 渠道 | 这次最有效的岗位来源是什么？为什么？ |
| 材料 | 哪份材料最影响推进结果？下次要怎么改？ |
| 面试 | 这次面试最关键的问题是什么？我的回答哪里可以更好？ |
| 节奏 | 哪个节点最容易拖延或遗漏？下次要提前做什么？ |
| 岗位方向 | 哪类岗位反馈最好？它和我的优势是否匹配？ |
| 下一步 | 下一轮投递最应该改变的一件事是什么？ |

#### 7.1.6 States

必须包含这些状态：

- 空状态：展示 3 条推荐问题和加号入口。
- 编辑中：问题、回答、备注可输入。
- 已保存：展示问题、回答摘要、备注摘要、更新时间。
- 已展开：查看完整回答和备注。
- 错误态：问题或回答为空时提示补全。
- 刷新后恢复：本地保存的 QA 仍然可见。

### 7.2 Key Features

#### 7.2.1 P0 Features

| Feature | Description | Acceptance Criteria |
|---|---|---|
| 明确复盘入口 | 在复盘中心首屏出现“开始本轮复盘”区域 | 用户进入页面即能看到填写入口 |
| 产品预设问题 | 系统给出一组可直接回答的问题 | 至少提供 6 个问题，覆盖渠道、材料、面试、节奏、方向、下一步 |
| QA 结构化输入 | 每条复盘由问题、回答、备注组成 | 问题和回答必填，备注选填 |
| 加号新增 QA | 用户可点击加号添加新问题 | 新增后立即出现可编辑 QA 卡片 |
| 保存与展示 | 用户保存后，QA 进入复盘记录区 | 已保存记录展示问题、回答摘要、备注摘要和时间 |
| localStorage 持久化 | 保存用户输入 | 刷新后记录不丢失 |
| Agent 记忆更新 | 复盘内容影响策略记忆摘要 | 至少能展示 1 条来自 QA 的策略记忆 |

#### 7.2.2 P1 Features

| Feature | Description | Value |
|---|---|---|
| 关联岗位 | QA 可绑定某个岗位 | 让复盘回到具体申请上下文 |
| 标签分类 | QA 可选择渠道、材料、面试、节奏等标签 | 方便后续筛选和策略提炼 |
| 一键生成下一步 | 根据 QA 生成一条行动建议 | 把复盘闭环到任务 |
| 复盘问题推荐 | Agent 根据当前风险和阶段推荐问题 | 让问题更贴合用户当前状态 |

#### 7.2.3 P2 Features

| Feature | Description |
|---|---|
| LLM 复盘总结 | 用模型把多条 QA 总结成周报 |
| 复盘历史时间线 | 按周或投递批次查看复盘 |
| 导出复盘报告 | 导出为 Markdown 或 PDF |
| 复盘到简历建议 | 根据复盘内容建议材料修改方向 |

### 7.3 Technology

本轮继续使用现有技术路线：

- Next.js App Router。
- TypeScript。
- Tailwind CSS v4。
- shadcn/ui。
- React Context + localStorage。
- 规则型 Agent / deterministic runtime。

建议新增或扩展的数据类型：

```typescript
type ReviewQuestionCategory =
  | "channel"
  | "material"
  | "interview"
  | "pace"
  | "direction"
  | "next_step"
  | "custom";

interface ReviewQA {
  id: string;
  question: string;
  answer: string;
  note: string;
  category: ReviewQuestionCategory;
  relatedJobId: string | null;
  isPreset: boolean;
  createdAt: string;
  updatedAt: string;
}
```

建议新增或调整的文件：

| File | Change |
|---|---|
| `src/lib/types.ts` | 增加 `ReviewQA` 和 `ReviewQuestionCategory` |
| `src/lib/mock-data.ts` | 增加默认复盘问题 |
| `src/lib/storage.ts` | 增加复盘 QA 的 localStorage 读写 |
| `src/hooks/use-jobfind-store.tsx` | 增加 reviewQAs 状态和增删改方法 |
| `src/lib/review-insights.ts` | 让 Agent 记忆读取用户 QA |
| `src/components/review/review-qa-composer.tsx` | 新增 QA 输入区 |
| `src/components/review/review-qa-list.tsx` | 展示已保存 QA |
| `src/app/review/page.tsx` | 调整页面结构，前置输入区 |

### 7.4 Assumptions

| Assumption | Why It Matters | How To Validate Later |
|---|---|---|
| 用户愿意回答短问题，而不是写长总结 | 决定 QA 结构是否成立 | 观察用户是否能在 2 分钟内保存第一条 QA |
| 产品预设问题能降低复盘门槛 | 决定默认问题质量 | 比较空白输入和预设问题的完成率 |
| 备注字段有价值 | 决定是否保留三段式结构 | 观察备注是否被真实填写 |
| 本地持久化足够支撑原型 | 决定是否需要后端 | 演示阶段验证刷新和重复访问 |
| Agent 记忆需要引用用户输入 | 决定复盘和 Agent 是否闭环 | 检查保存 QA 后策略记忆是否变化 |

## 8. Release

### 8.1 V2 First Version Scope

V2 第一版只做“复盘输入路径显性化”和“QA 结构化沉淀”。

必须包含：

- 页面标题升级为“迭代复盘中心”。
- 首屏明确出现复盘输入区。
- 产品预设问题。
- 问题填写栏。
- 回答填写栏。
- 备注填写栏。
- 加号新增 QA。
- 保存后进入记录列表。
- localStorage 持久化。
- Agent 记忆至少读取一条 QA 结论。

可以暂缓：

- LLM 自动总结。
- 导出报告。
- 复杂筛选。
- 多人协作。
- 后端保存。

### 8.2 Suggested Build Plan

| Phase | Focus | Output |
|---|---|---|
| Phase 1 | 数据结构和存储 | `ReviewQA` 类型、默认问题、localStorage 读写 |
| Phase 2 | 输入组件 | QA 输入卡片、加号新增、保存校验 |
| Phase 3 | 页面整合 | 复盘中心首屏前置输入区，记录列表展示 |
| Phase 4 | Agent 记忆联动 | `review-insights` 读取 QA 并生成策略记忆 |
| Phase 5 | 验证和打磨 | 测试保存、刷新、空状态、错误态和演示路径 |

### 8.3 Acceptance Criteria

1. 进入复盘中心后，首屏能看到“开始本轮复盘”区域。
2. 页面至少展示 6 条产品预设问题。
3. 用户可以填写问题、回答和备注。
4. 用户可以点击加号新增一条自定义 QA。
5. 问题和回答为空时不能保存，并给出清晰提示。
6. 保存后，记录出现在已保存 QA 列表。
7. 刷新页面后，已保存记录仍然存在。
8. Agent 策略记忆能展示基于用户 QA 的建议。
9. 现有渠道复盘、面试复盘和转化漏斗仍然可见。
10. 不引入后端、登录、模型 API 或复杂富文本编辑器。

### 8.4 Demo Script

1. 打开“迭代复盘中心”，说明这里不是只看数据，而是把经验沉淀为下一轮策略。
2. 展示产品预设问题：“这次面试最关键的问题是什么？”
3. 填写回答：“被问到如何判断需求优先级，我回答偏流程，缺少指标。”
4. 填写备注：“下次补充 DAU、转化率、候选方案成本。”
5. 点击保存，QA 出现在复盘记录。
6. 点击加号，新增自定义问题：“下一轮投递要减少哪类岗位？”
7. 保存后展示 Agent 策略记忆更新：下次面试前先准备指标化需求排序案例。
8. 下滑查看渠道复盘和漏斗，说明定量数据和用户复盘会一起影响策略。

### 8.5 Risks And Mitigations

| Risk | Impact | Mitigation |
|---|---|---|
| 输入区太重，页面变复杂 | 用户不愿开始 | 首屏只展示 1 个主输入区，其余问题折叠或列表化 |
| 预设问题太泛 | 用户觉得没有帮助 | 问题覆盖渠道、材料、面试、节奏、方向和下一步 |
| QA 只保存不产生价值 | 复盘像普通笔记 | Agent 记忆必须读取 QA 并生成下一步建议 |
| 加号入口不明显 | 用户不知道能新增 | 加号放在输入区标题右侧，并使用明确文案 |
| 本地数据丢失 | 演示不稳定 | 统一走现有 storage 封装，并补测试 |

## Appendix: Positioning Statement

迭代复盘中心面向正在持续求职的学生。它把渠道结果、面试问题、材料表现和用户自己的判断沉淀成结构化 QA，再由 Agent 提炼成下一轮求职策略。

它不是普通复盘笔记，而是 JobFind 从“今日行动管理”走向“持续策略迭代”的关键模块。
