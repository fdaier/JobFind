# JobFind 组件选型文档

> 本文档记录 JobFind 项目中每个 UI 场景的组件选择、选用理由、备选方案及不选原因。
> UI 框架：shadcn/ui（基于 Radix UI + Tailwind CSS）

---

## 1. 全局导航：Sidebar

### 选择：`Sidebar`

| 维度 | 说明 |
|------|------|
| **场景** | 全局页面切换（仪表盘、看板、材料中心） |
| **选用理由** | ① Dashboard 类产品的行业标准导航方式；② shadcn 提供完整的 Sidebar 组件，含折叠、分组、图标支持；③ 始终可见，用户不需要猜页面在哪里；④ 后续扩展页面（设置、帮助）时无需改布局 |
| **备选方案** | Tabs（顶部标签页）、Navigation Menu（顶部菜单） |
| **不选理由** | Tabs 更适合同页面内切换视图，不适合跨页面导航；Navigation Menu 适合营销站点，Dashboard 产品几乎不用 |

---

## 2. 岗位详情：Sheet（侧边抽屉）

### 选择：`Sheet`（side="right", 宽度 ~640px）

| 维度 | 说明 |
|------|------|
| **场景** | 从看板点击岗位卡片，查看岗位完整信息 |
| **选用理由** | ① 保持看板在背景可见，用户不会"丢失上下文"；② 右侧滑入是 CRM/看板产品的成熟交互模式（如 Linear、Notion）；③ 内容区域足够宽，可以放 Tabs 切换多个信息维度；④ 关闭方式直觉（点击遮罩或 X） |
| **备选方案** | Dialog（居中弹窗）、Drawer（底部抽屉）、独立页面跳转 |
| **不选理由** | Dialog 会遮挡看板，且宽度受限难放复杂内容；Drawer 从底部滑入不符合桌面端习惯；独立页面跳转会打断看板浏览流程 |

---

## 3. JD 解析入口：Dialog

### 选择：`Dialog`（居中弹窗）

| 维度 | 说明 |
|------|------|
| **场景** | 用户点击"粘贴 JD 添加岗位"，输入 JD 文本，查看解析结果，确认保存 |
| **选用理由** | ① JD 解析是一个聚焦的 wizard 流程（输入→解析→预览→保存），Dialog 能引导用户专注完成；② 不需要看到背景内容；③ 步骤完成后自动关闭，干净利落；④ Dialog 在 shadcn 中成熟度高，支持自定义宽度和嵌套滚动 |
| **备选方案** | Sheet（侧边抽屉）、独立页面 |
| **不选理由** | Sheet 已被岗位详情占用，同时出现两个 Sheet 会产生遮挡冲突；独立页面太重，一个粘贴动作不值得跳转 |

---

## 4. 统计指标：Card

### 选择：`Card`

| 维度 | 说明 |
|------|------|
| **场景** | 首页顶部 4 个统计指标（本周投递数、待处理任务、高风险申请、即将面试） |
| **选用理由** | ① Card 提供容器化的视觉分区，每个指标独立成块；② shadcn Card 内置 CardHeader/CardTitle/CardContent 结构，语义清晰 ；③ 可以添加图标和趋势箭头，视觉丰富；④ 响应式排列简单（grid 4列→2列→1列） |
| **备选方案** | 纯 div 自定义样式 |
| **不选理由** | 自定义样式需要更多 CSS，且缺少语义化结构，不如 Card 组件一致 |

---

## 5. 风险和阶段标签：Badge

### 选择：`Badge`

| 维度 | 说明 |
|------|------|
| **场景** | ① 岗位卡片上的风险标签（红/黄/绿）；② 阶段标签（感兴趣/已投递/面试中等）；③ 材料类型标签 |
| **选用理由** | ① Badge 是信息标注的标准组件，用户一眼识别；② shadcn Badge 支持多种 variant（default/secondary/destructive/outline），可以映射到不同风险等级；③ 体积小，不占用卡片空间；④ 可自定义颜色扩展 |
| **备选方案** | 纯文字 + 颜色、Tooltip |
| **不选理由** | 纯文字没有视觉容器包裹，在密集信息中不够突出；Tooltip 需要 hover 才能看到，风险信息应该直接可见 |

---

## 6. 详情内信息切换：Tabs

### 选择：`Tabs`

| 维度 | 说明 |
|------|------|
| **场景** | 岗位详情抽屉内切换 5 个信息维度（基本信息/时间线/材料/AI建议/面试记录） |
| **选用理由** | ① 5 个维度信息量差异大，逐个展示比全部堆叠更清晰；② Tabs 切换即时，无加载感；③ 用户可以快速定位到需要的信息；④ shadcn Tabs 与 Sheet 嵌套兼容良好 |
| **备选方案** | Accordion（折叠展开）、ScrollArea 全部平铺 |
| **不选理由** | Accordion 适合 2-3 个简短内容分组，5 个维度会导致页面很长且频繁展开/折叠；全部平铺在 640px 宽度的 Sheet 中会造成信息过载 |

---

## 7. 材料完整度：Progress

### 选择：`Progress`

| 维度 | 说明 |
|------|------|
| **场景** | 岗位卡片和详情页展示该岗位材料的完整百分比 |
| **选用理由** | ① 进度条比数字更直觉（"还差多少"一目了然）；② 可以用颜色编码（100% 绿、50-99% 黄、<50% 红）强化信息；③ shadcn Progress 组件简洁，占用空间小 |
| **备选方案** | 纯百分比文字、环形图 |
| **不选理由** | 纯文字失去视觉冲击力；环形图在卡片中太大，且 shadcn 没有原生圆环进度 |

---

## 8. JD 文本输入：Textarea

### 选择：`Textarea`

| 维度 | 说明 |
|------|------|
| **场景** | JD 解析 Dialog 中用户粘贴职位描述文本 |
| **选用理由** | ① JD 是多行长文本，需要多行输入区域；② shadcn Textarea 样式与系统整体一致；③ 支持 placeholder 引导用户操作 |
| **备选方案** | Input、富文本编辑器 |
| **不选理由** | Input 是单行的；富文本编辑器过度设计，JD 只需要纯文本粘贴 |

---

## 9. 内容滚动：Scroll Area

### 选择：`Scroll Area`

| 维度 | 说明 |
|------|------|
| **场景** | ① 看板列内岗位数量超出视口时；② 详情抽屉内长内容；③ 风险雷达列表 |
| **选用理由** | ① 替代浏览器原生滚动条，提供更精致的视觉效果（细滚动条 + hover 可见）；② 在 Sheet/Dialog 嵌套中表现稳定；③ 跨浏览器一致 |
| **备选方案** | CSS `overflow-auto`（原生滚动） |
| **不选理由** | 原生滚动条在 Windows 上很宽且不美观，影响 Dashboard 精致感 |

---

## 10. 操作反馈：Sonner（Toast）

### 选择：`Sonner`

| 维度 | 说明 |
|------|------|
| **场景** | 保存岗位、推进阶段、标记任务完成等操作后的反馈 |
| **选用理由** | ① Sonner 是 shadcn 推荐的 Toast 方案（取代旧 Toast 组件）；② 动画流畅，支持堆叠；③ API 极简（`toast("保存成功")`）；④ 支持 success/error/info 等预设样式 |
| **备选方案** | shadcn Toast（旧版）、Alert |
| **不选理由** | shadcn 已标注 Sonner 为替代方案；Alert 不会自动消失，需要用户手动关闭 |

---

## 11. 信息提示：Tooltip

### 选择：`Tooltip`

| 维度 | 说明 |
|------|------|
| **场景** | ① 风险标签的详细解释；② 统计数据的计算说明；③ 侧边栏折叠时的菜单项名称 |
| **选用理由** | ① hover 即显示，不打断操作流程；② 适合放次要但用户可能好奇的解释性文案；③ shadcn Tooltip 自带箭头和定位 |
| **备选方案** | Hover Card、Popover |
| **不选理由** | Hover Card 适合预览卡片级别内容，不适合一行文字；Popover 需要点击且会遮挡 |

---

## 12. 视觉分隔：Separator

### 选择：`Separator`

| 维度 | 说明 |
|------|------|
| **场景** | 面板内不同区块之间的视觉间隔 |
| **选用理由** | ① 比 `border-bottom` 更语义化；② 与 shadcn 主题色自动匹配；③ 支持横向和纵向 |
| **备选方案** | CSS border、空白间距 |
| **不选理由** | CSS border 需要手动匹配颜色；空白间距在信息密集场景中不够明确 |

---

## 13. 按钮和交互：Button

### 选择：`Button`

| 维度 | 说明 |
|------|------|
| **场景** | 所有可点击操作（保存、推进阶段、添加岗位、标记完成等） |
| **选用理由** | ① 统一交互元素视觉；② 支持 variant（default/outline/ghost/destructive）映射不同操作重要性；③ 支持 size 和 loading 状态 |
| **用法规范** | 主操作用 default variant，次操作用 outline/ghost，危险操作用 destructive |

---

## 不使用的组件及理由

| 组件 | 不选理由 |
|------|---------|
| Accordion | 详情抽屉用 Tabs 更清晰；首页无折叠需求 |
| Alert Dialog | 首版没有危险操作确认需求（不删除数据） |
| Calendar | 首版 DDL 展示用文本格式，不需要日历选择器 |
| Carousel | 无轮播内容需求 |
| Checkbox | 任务完成用按钮交互，不用复选框 |
| Combobox | 首版无需搜索+选择的复合输入 |
| Command | 首版不做全局搜索/命令面板 |
| Context Menu | 首版不做右键菜单 |
| Data Table | 首版看板以卡片为主，不用表格视图 |
| Date Picker | 首版日期来自数据，不需要用户手动选日期 |
| Drawer | 已选 Sheet 做岗位详情，不需要底部抽屉 |
| Dropdown Menu | 首版没有复杂下拉菜单场景 |
| Input | JD 输入用 Textarea，其他表单首版不做 |
| Menubar | 不用菜单栏导航 |
| Pagination | 数据量小（8 条），不需要分页 |
| Popover | 用 Tooltip 覆盖了信息提示需求 |
| Radio Group | 首版没有单选场景 |
| Resizable | 首版布局固定，不需要用户调节区域大小 |
| Select | 阶段推进可用 Button Group 或简单列表替代 |
| Skeleton | 数据来自本地，无真实加载态，不需要骨架屏 |
| Slider | 无滑动选择需求 |
| Switch | 无开关切换设置 |
| Table | 看板场景以卡片为主，材料中心也用卡片 |
| Toggle / Toggle Group | 首版无视图切换需求 |

---

## 汇总：需要安装的 shadcn 组件

```bash
npx shadcn@latest add badge button card dialog progress scroll-area separator sheet sidebar sonner tabs textarea tooltip
```

共 **14 个组件**，覆盖所有 P0 + P1 场景。

---

## 组件使用全景图

```
┌─────────────────────────────────────────────────────────────────┐
│  App Layout                                                      │
│  ┌──────────┐  ┌────────────────────────────────────────────┐   │
│  │ Sidebar  │  │  Page Content                               │   │
│  │          │  │  ┌─────────────────────────────────────────┐│   │
│  │ · 仪表盘  │  │  │ Dashboard                               ││   │
│  │ · 看板    │  │  │  Card×4 (Stats)                         ││   │
│  │ · 材料    │  │  │  ┌──────────┐┌──────────┐┌───────────┐ ││   │
│  │          │  │  │  │TodayTasks││MiniKanban││RiskRadar  │ ││   │
│  │ Tooltip  │  │  │  │Badge     ││Badge     ││Badge      │ ││   │
│  │ (折叠态) │  │  │  │Button    ││          ││Tooltip    │ ││   │
│  │          │  │  │  └──────────┘└──────────┘└───────────┘ ││   │
│  │          │  │  │  FunnelChart                            ││   │
│  │          │  │  └─────────────────────────────────────────┘│   │
│  │          │  │                                              │   │
│  │          │  │  ┌─────────────────────────────────────────┐│   │
│  │          │  │  │ Board                                    ││   │
│  │          │  │  │  KanbanColumn×7                          ││   │
│  │          │  │  │   └─ JobCard (Card+Badge+Progress)       ││   │
│  │          │  │  │  Button("粘贴JD") → Dialog               ││   │
│  │          │  │  │   └─ Textarea + Button → Result Card     ││   │
│  │          │  │  └─────────────────────────────────────────┘│   │
│  │          │  │                                              │   │
│  │          │  │  ┌─────────────────────────────────────────┐│   │
│  │          │  │  │ JobDetailSheet (Sheet)                    ││   │
│  │          │  │  │  Tabs                                    ││   │
│  │          │  │  │   ├─ Info (ScrollArea)                   ││   │
│  │          │  │  │   ├─ Timeline (Separator between items)  ││   │
│  │          │  │  │   ├─ Materials (Progress + Badge)        ││   │
│  │          │  │  │   ├─ AI Panel (Card + Button)            ││   │
│  │          │  │  │   └─ Interview Notes (Card)              ││   │
│  │          │  │  │  Button("推进阶段")                       ││   │
│  │          │  │  └─────────────────────────────────────────┘│   │
│  └──────────┘  └────────────────────────────────────────────┘   │
│  Sonner (Toast, 全局右下角)                                      │
└─────────────────────────────────────────────────────────────────┘
```
